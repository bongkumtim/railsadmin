<%= form_for(income_tax) do |f| %>
  <% if income_tax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(income_tax.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% income_tax.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h3> Title</h3>
  <select name="income_tax[title]">
  <option value="Insurance Premium Receipt">&nbsp;&nbsp;Insurance Premium Receipt</option>
  <option value="Book Purchase Receipt">Book Purchase Receipt</option>
  <option value="Medical Receipt">Medical Receipt</option>
  <option value="Donation Receipt">Donation Receipt</option>
  <option value="Zakat Receipt">Zakat Receipt</option>
  <option value="Personal Computer Receipt">Personal Computer Receipt</option>
  <option value="Sport Equipment Receipt">Sport Equipment Receipt</option>
  <option value="Others">Others</option>
</select>
<br /><br />
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <br />
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>

  <div class='field'><br />
  <%= f.file_field :image %>
  </div>
<br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>