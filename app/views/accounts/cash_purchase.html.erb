<div class="container">
<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= f.label :cash_purchase_amount %><br />
    RM <%= f.number_field :cash_purchase_amount %>
 </div>
 <br />
  <div class="field">
    <%= f.label :cash_purchase_description %><br />
    <p>*brand + prouduct (eg: Logitech Wireless Mouse)</p>
    Product : <%= f.text_field :cash_purchase_description %> 
  </div>
  <br />
  <div class="field">
    <%= f.label :Receipt %><br />
    <%= f.file_field :image %> *Needed for Accounting Record Purposes
  </div>
  <br />
    <div class="actions">
    <%= f.submit %> | <%= link_to 'Back', accounts_path %>
  </div>
<% end %>
</div>

