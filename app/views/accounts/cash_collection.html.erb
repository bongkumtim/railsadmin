<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cash_collection_amount %><br />
    RM <%= f.text_field :cash_collection_amount %>
  </div>

  <div class="field">
    <%= f.label :cash_collection_description %>
    <p> eg: Sales of keyboard </p>
    Sales :<%= f.text_field :cash_collection_description %>
  </div>
  <br />
    <div class="actions">
    <%= f.submit %>  | <%= link_to 'Back', accounts_path %>
  </div>
<% end %>

